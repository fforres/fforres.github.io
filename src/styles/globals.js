import { css, after, hover, merge } from 'glamor';

export const row = css({
  marginRight: '-15px',
  marginLeft: '-15px',
});

export const center = css({
  display: 'flex',
  alignItems: 'center',
  flexDirection: 'column',
});

export const section = css({
  paddingTop: '3em',
  paddingBottom: '3em',
});
